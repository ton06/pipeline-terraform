name: "Deploy Windrose Server"

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    uses: ton06/workflows/.github/workflows/deploy.yml@main
    secrets: inherit
    with:
      environment: main
      aws-region: "sa-east-1"
